{
  "name": "garage-assistant-mcp",
  "version": "1.0.0",
  "description": "MCP server for controlling Garage Assistant 4 (FileMaker) via Parallels",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && npm run build:ocr",
    "build:ocr": "mkdir -p dist/native && swiftc -O native/ocr.swift -o dist/native/ocr",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
